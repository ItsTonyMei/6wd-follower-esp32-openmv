# ROADMAP — 履带车视觉跟随系统

## 架构总览

```
┌──────────────────────────────────────────────────────────────┐
│                     L1: 感知层 (OpenMV)                       │
│   YOLO person detection + VL53L1X ToF 激光测距 → 视觉+ToF 融合  │
│   距离估计 → VIS/UART → ESP32                                  │
├──────────────────────────────────────────────────────────────┤
│                     L2: 决策层 (ESP32)                        │
│   FollowLogic + 行为仲裁 + WiFi Dashboard + 远程调参          │
│   接收: OpenMV VIS帧 + 遥控SBUS + STM32遥测                   │
│   输出: MotorCmd → UART → STM32                              │
├──────────────────────────────────────────────────────────────┤
│                   L3: 执行与安全层 (STM32)                     │
│   PWM电机驱动 + 编码器PID + 命令超时停车 + 硬线急停 + 过流保护  │
│   独立于L1/L2的安全链路，任何上层故障均可自主切断动力            │
└──────────────────────────────────────────────────────────────┘
```

## 安全链路设计（5条独立关断路径）

| # | 触发源 | 机制 | 响应时间 |
|---|--------|------|---------|
| 1 | 人按急停按钮 | 继电器物理断开电机电源 | <10ms |
| 2 | STM32 检测过流 | ADC阈值 → PWM归零 + 错误码上报 | <1ms |
| 3 | STM32 命令超时 | 500ms无有效命令 → 自动STOP | <500ms |
| 4 | ESP32 遥控失联 | SBUS failsafe flag → STOP | <100ms |
| 5 | ESP32 视觉超时 | VISION_TIMEOUT_MS → 降级STOP或切Manual | <700ms |

## 数据流

```
OpenMV                        ESP32                          STM32
───────                      ───────                        ───────
YOLO检测                      VisionBridge.parse()           UART1 RX中断
  │                             │                              │
  ├── VL53L1X ToF (I2C)         ▼                              ▼
  │     └→ 距离(mm)           VisState更新                   MotorCmd解析
  ▼                             │                           (CRC8校验)
视觉+ToF融合距离               ▼                              │
  │                           FollowLogic.update()             ▼
  ▼                             · 距离保持 (distScore)        电机PWM输出
VIS帧组装                       · 行为仲裁                    │
  │                             │                              ▼
UART3.write() ──────────────►   ▼                           编码器ADC采集
                               MotorCmd + CRC8 ──────────────► ▼
                                                            遥测帧组装
                               ◄──────────────────────  (编码器/电流/错误)
                               │
                               ▼
                             DataAggregator
                               │
                               ▼
                             Dashboard WebSocket (WiFi)
```

---

## Phase 0 — 项目初始化与硬件验证 ✅ 核心完成

**目标**: 所有板子能独立跑起来，硬件连接确认，电气方案确定。
**状态**: STM32 ✅ | OpenMV N6 ✅ | ESP8266 Bridge ✅ | ESP32 ⚠️ (Serial2 异常, Phase 1+ 待换板)

> **通信方案**: N6 P0 SW UART @ 4800bd → ESP8266 D5 SoftwareSerial → WiFi Dashboard (97%+ 成功率)
> **ESP32 Serial2 (GPIO16/17) 硬件不稳定, Phase 0 VIS 接收+Dashboard 由 ESP8266 桥接**
> **待推进**: Phase 0.5 电气拓扑, 0.6 原装系统逆向 (Phase 1 可并行)

### 0.1 STM32F103C8T6 开发环境搭建 ✅ 完成
- [x] 安装 STM32duino (Arduino_Core_STM32) 或 PlatformIO
- [x] USB-TTL 烧录接线确认 (PA9/PA10, CH9102 USB-UART via COM11)
- [x] 芯片丝印确认: **STM32F103C8T6** (Cortex-M3 @ 72MHz, 64KB Flash, 20KB SRAM) — 定制板, 非 Blue Pill
- [x] **GPIO 外设扫描完成** — 30 个 GPIO 逐一轮询确认:
  - **PA3 = BEEP 蜂鸣器** (active-LOW, 2-pin 排针引出)
  - **PA4 = LED2 蓝灯** (active-LOW, 用户可编程)
  - LED1/LED3/LED4 = 电源指示灯 (红色, 非 GPIO 控制)
  - Serial: USART1 (PA9=TX, PA10=RX), generic 变体默认 USART2 需 `HardwareSerial` 覆盖
- [x] **Blink 验证通过** — PA4 LED2 蓝灯 500ms 闪烁, 烧录 115200bps 序列 `-dtr,rts,dtr,,,,`

### 0.2 ESP32-WROOM-32U 现有代码模块化梳理
- [x] 芯片丝印确认: **ESP32-WROOM-32U** (ESP32-D0WD-V3 rev3.1, 240MHz, 4MB Flash)
- [x] 串口通信确认: COM9, CP210x @ 115200
- [x] **固件清理完成**: 移除 UltrasonicSensors, MotorDriver, MotorTask, Tasks 旧文件
- [x] **Clean Boot 验证**: WiFi AP 启动, Dashboard 在线, 无重启循环
- [x] **Dashboard 测试通过**: 手机端深色主题, `/status` JSON 接口, 200ms 轮询
- [ ] 标记 FollowLogic → 保留在 ESP32，需重构为差速转向
- [ ] 标记 VisionBridge → 保留，无需改动
- [ ] 标记 DataAggregator → 保留，新增 STM32 遥测字段 + VisState tofDistance

### 0.3 OpenMV 现有代码验证 ✅ 完成
- [x] 在 OpenMV N6 上运行 `OpenMV/main.py` — **YOLOv8n NPU 推理, 45 FPS**
- [x] 确认 YOLO person detection 正常输出 — confidence 0.50-0.92
- [x] VL53L1X 测距扩展板安装 + I2C 扫描确认设备在线 (addr 0x29) — **N6 I2C(2) 已通过**
- [x] 运行 `OpenMV/test_vl53l1x.py` 验证 — **100/100 有效, ~2.9m, 33.3Hz, ±25mm 抖动**
- [x] VIS 帧输出验证 — **P0 SW UART @ 4800 → ESP8266 D5, 97.4% 成功率**
- [x] 帧率记录: **YOLOv8n NPU=45FPS** (vs 旧 YOLO-LC CPU=~10FPS, 提升 4.5x)

> **N6 配置**: YOLOv8n @ `/rom/yolov8n_192.tflite` + YoloV8 postprocess, NPU accelerated
> VL53L1X → I2C(2) (SCL=P4, SDA=P5), addr 0x29, Model ID 0xEACC
> VIS → P0 SW UART @ 4800 baud → ESP8266 Bridge D5 (GPIO14)
> **N6 绘图 API**: 使用 tuple 坐标 (不用分开的 x,y 参数)

### 0.3b ESP8266 Bridge 验证 ✅ 完成
- [x] SoftwareSerial D5(GPIO14) @ 4800 接收 N6 VIS 帧 — **97.4% 成功率**
- [x] WiFi AP "Tracked Robot" + HTTP Dashboard (:80) — 手机端验证通过
- [x] `/status` JSON API 实时更新 VIS 数据
- [x] 注意: ESP32 Serial2 (GPIO16/17) 硬件不稳定, 暂由 ESP8266 桥接

### 0.4 硬件清单确认与采购
| 硬件 | 数量 | 规格 | 用途 | 状态 |
|------|------|------|------|------|
| STM32F103C8T6 (定制板) | 1 | Cortex-M3 72MHz, 64KB Flash, 20KB SRAM | 实时控制主板 | **已有** (丝印+烧录确认) |
| ESP32-WROOM-32U (DevKit V1) | 1 | Xtensa LX6 双核 240MHz, 4MB Flash | 决策 + WiFi | **已有** (丝印+Dashboard确认) |
| OpenMV Cam N6 | 1 | Neural-ART NPU, 45FPS YOLOv8n | 视觉感知 | **已有** (YOLO+ToF+VIS验证) |
| VL53L1X ToF 测距模块 | 1 | I2C, 40-4000mm, ±1mm, 50Hz | 激光测距 | **已有** (N6 I2C2验证) |
| ESP8266 NodeMCU V3 | 1 | ESP-12E, 80MHz, WiFi | VIS 桥接 + Dashboard (Phase 0) | **已有** (Bridge验证) |
| 履带底盘 (含电机) | 1 | 金属履带, 载重 100kg | 执行平台 | **已到货** |
| 防水 48V 双向双路差速有刷电调 | 1 | 48V 45A 500W ×2ch | 左右履带独立驱动 | 已确认待装 |
| 防水 48V→5V 10A 降压模块 | 1 | 防水, 10A | 控制电路供电 | 已确认待装 |
| 48V 锂电池 | 1 | 48V 89Ah | 动力电源 | **随车已有，保留** |
| 原装供电模块 | 1 套 | 原车配置 | 配电/保险/走线 | 保留 |
| Jumper T14 ELRS 遥控器 | 1 | EdgeTX, ELRS 2.4G 1W | 遥控发射端 | ⏳ 待采购 |
| ELRS 2.4G 接收机 (ER6/EP1) | 1 | CRSF 协议, 双向遥测 | 遥控接收端 → ESP32 UART1 | ⏳ 待采购 |
| 急停按钮 (蘑菇头) | 2 | 防水, 常闭触点 | 前后各一 | ⏳ 待采购 |
| 48V 大电流继电器 | 1 | 48V ≥60A | 硬线急停切断动力 | ⏳ 待采购 |
| 电流传感器 (ACS712) | 2 | ±30A 或更大 | 左右电机电流监控 | △ 待确认电调是否自带 |
| 磁编码器 (AS5600) | 2 | — | 履带速度反馈(可选) | 可选 |

### 0.5 整车电气拓扑图

```
                         ┌───────────────────────────────────────────┐
                         │          汽车蓄电池 48V 89Ah              │
                         │          ═══════════════                  │
                         └────┬──────────┬──────────┬───────────────┘
                              │          │          │
                    ┌─────────┘          │          └─────────┐
                    │ 动力正极 (粗红)     │          动力正极 (粗红)  │
                    ▼                    ▼                    ▼
              ┌──────────┐     ┌──────────────┐     ┌──────────────┐
              │ 急停继电器 │     │ 48V→5V 10A  │     │ 双向双路     │
              │ (常闭 NC) │     │ 防水降压模块 │     │ 有刷电调     │
              │ ≥60A     │     │ 输入: 48V    │     │ 输入: 48V    │
              └────┬─────┘     │ 输出: 5V 10A │     │ 2ch×45A×500W│
                   │           └──────┬───────┘     └──┬────┬──────┘
                   │ 受控端          │                │    │
                   │ (48V 输出)      │ 5V 输出        │    │
                   ▼                 │           M1 左履带 M2 右履带
              ┌────────────┐         │          (粗线到电机)
              │    ⚠       │         │
              │ 继电器断开  │         ├────────────┬────────────┐
              │ = 电调无电  │         ▼            ▼            ▼
              │ = 紧急停车  │   ┌──────────┐ ┌──────────┐      │
              └─────┬──────┘   │ STM32    │ │ ESP8266  │      │
                    │          │ (自制板) │ │ NodeMCU  │      │
                    │          │ DC 5V    │ │ MicroUSB │      │
                    ▼          │ 或 USB-C │ │ 5V       │      │
              电调 48V 输入    └────┬─────┘ └────┬─────┘      │
              (经过继电器)          │            │            │
                                   │ UART3      │ UART0      │
                                   │ PB10/PB11  │            │
                                   └──┬──┬──────┘            │
                                      │  │                   │
                         ESP32        │  │                   │
                    (Phase 1+ 待接入)  │  │                   │
                                      │  │                   │
                                   ┌──┘  └───────────────────┘
                                   │     VIS: N6 P0→ESP8266 D5
                                   │     (Phase 0 桥接)
                                   │
                                   ▼
                            ┌──────────────┐
                            │  OpenMV N6   │
                            │  MicroUSB 5V │
                            │  (独立供电)   │
                            └──────────────┘

     ┌──────────────────────────────────────────────────────┐
     │  ⚠ 电压域隔离                                       │
     │  48V 动力地 ────┐                                   │
     │  5V 信号地  ────┼── 单点接地 (电池负极汇流点)       │
     │  3.3V 逻辑地 ───┘                                   │
     │  ▸ 严禁 48V 与 5V/3.3V 共用地线走线                  │
     └──────────────────────────────────────────────────────┘
```

#### STM32 完整引脚分配

```
  ╔══════════════════════════════════════════════════════════════╗
  ║              STM32F103C8T6  定制板  接口分配                ║
  ╠══════════════════════════════════════════════════════════════╣
  ║                                                            ║
  ║  ┌──── ┬────┬────┬────┬────┬────┬────┬────┬────┬────┐     ║
  ║  │ PB9 │PB8 │ PB7│ PB6│ PB5│ PB4│ PB3│PA15│PA12│PA11│     ║
  ║  │TIM4 │TIM4│I2C │I2C │    │    │    │    │    │    │     ║
  ║  │ CH4 │CH3 │SDA │SCL │    │    │    │    │    │    │     ║
  ║  │  ESC│ ESC│ 预 │ 预 │    │    │    │    │    │    │     ║
  ║  │ CH2 │ CH1│ 留 │ 留 │    │    │    │    │    │    │     ║
  ║  ├────┼────┼────┼────┼────┼────┼────┼────┼────┼────┤     ║
  ║  │PA10│PA9 │PA8 │PC13│PB15│PB14│PB13│PB12│PB11│PB10│     ║
  ║  │US1 │US1 │急停│    │    │    │    │继电│US3│US3│     ║
  ║  │ RX │ TX │输入│    │    │    │    │器控│ RX│ TX│     ║
  ║  │PC端│PC端│◄── │    │    │    │    │──► │◄ESP│→ESP│     ║
  ║  └────┴────┴────┴────┴────┴────┴────┴────┴────┴────┘     ║
  ║                                                            ║
  ║  ┌────┬────┬────┬────┬────┬────┬────┬────┬────┬────┐     ║
  ║  │ PA0│ PA1│ PA2│ PA3│ PA4│ PA5│ PA6│ PA7│ PB0│ PB1│     ║
  ║  │ADC │ADC │US2 │US2 │LED2│    │TIM3│TIM3│TIM3│TIM3│     ║
  ║  │电流│电流│ TX │ RX │蓝灯│    │CH1 │CH2 │CH3 │CH4 │     ║
  ║  │ 左 │ 右 │ 蓝 │蜂鸣│ 状 │    │ 预 │ 预 │ 预 │ 预 │     ║
  ║  │◄── │◄── │牙  │器⚡│ 态 │    │ 留 │ 留 │ 留 │ 留 │     ║
  ║  └────┴────┴────┴────┴────┴────┴────┴────┴────┴────┘     ║
  ║                                                            ║
  ║  ⚡ PA3: 蜂鸣器跳线帽保留, 暂不配置代码 (Phase 1 后处理)   ║
  ║                                                            ║
  ╠══════════════════════════════════════════════════════════════╣
  ║  功能分组速查:                                              ║
  ║  ─────────                                                 ║
  ║  🔌 ESC PWM  │ PB8(CH1左履带)  PB9(CH2右履带)  TIM4 50Hz  ║
  ║  📡 ESP32   │ PB10(USART3_TX) PB11(USART3_RX)  115200bd  ║
  ║  💻 PC调试  │ PA9(USART1_TX)  PA10(USART1_RX)  115200bd  ║
  ║  ⚡ 电流ADC │ PA0(左电机)     PA1(右电机)      ACS712    ║
  ║  🛡 安全    │ PA8(急停输入)   PB12(继电器控制)           ║
  ║  💡 板载   │ PA4(LED2蓝灯)   PA3(蜂鸣器,暂不配)        ║
  ║  🔧 SWD    │ PA13(SWDIO)     PA14(SWCLK)               ║
  ║  📦 预留   │ PA6,PA7,PB0,PB1(TIM3) PB6,PB7(OLED原用)   ║
  ╚══════════════════════════════════════════════════════════════╝
```

#### ESC 接线详解 (PB8/PB9 → 电调)

```
     STM32 侧                             电调侧
  ════════════                         ════════════

  ┌──────────┐                         ┌──────────────┐
  │  PB8 ─────┐   白/黄 (PWM信号)      │  CH1 输入     │
  │  GND ───┐ │   黑/棕 (GND)          │              │
  │         │ └───→────────────────→   │              │
  │         └─────→────────────────→   │              │
  │                                    ├──────────────┤
  │  PB9 ─────┐   白/黄 (PWM信号)      │  CH2 输入     │
  │  GND ─────┘   黑/棕 (GND)          │              │
  └──────────┘                         └──────────────┘

     电调红线 (5V BEC 输出) → 悬空不接！
     ┌─────────────────────────────────────────────┐
     │ ⚠ STM32 引脚耐压 3.3V, ESC BEC 输出 5V      │
     │   接红线 = 5V 灌入 3.3V 轨 → 烧 IO 口或 LDO  │
     │   STM32 已由 48V→5V 降压模块供电, 不需 BEC   │
     └─────────────────────────────────────────────┘

     电调供电:
     汽车蓄电池 48V ──→ [急停继电器 NC] ──→ 电调 48V 输入
                       (继电器断开=电调失电=停车)
```

#### 上电时序与安全逻辑

```
  1. 48V→5V 降压器上电 → STM32 启动
  2. STM32 初始化:
     - PB8/PB9 配置为 TIM4_CH3/CH4, 输出 1500μs (STOP 中位)
     - PA8 配置为 GPIO 输入 + 内部上拉 (急停检测)
     - PB12 低电平 (继电器断开, 默认安全)
  3. STM32 读取 PA8:
     - HIGH (按钮未按) → 安全 → PB12 拉高 → 继电器吸合 → 48V 通入电调
     - LOW  (按钮按下) → 急停 → PB12 保持低 → 继电器断开
  4. 电调上电自检 (听确认音)
  5. 就绪, 可发运动 PWM 指令
  6. 运行时 PA8 中断触发 → ISR 内: PB12 拉低 + PB8/PB9 归中位
```

- [x] 整车电气拓扑已更新 (电调供电来自电池, 控制板供电来自降压模块)
- [x] STM32 引脚分配已确定 (PB8/PB9=PWM, PB10/PB11=ESP32, PA0/PA1=ADC)
- [ ] 确认原装供电模块的接口和走线，标记哪些保留哪些更换
- [ ] 标注电压域隔离（48V 动力地 与 5V 信号地 单点接地）
- [ ] 电调控制接口确认（查看电调说明书：RC PWM / UART / CAN / 模拟电压）
- [ ] 实测 ESC 信号电平（3.3V PWM 是否被电调正确识别，如需电平转换加 74LVC245）

### 0.5b 板间连接器与线束规范 (工业级接线方案)

> **目的**: 杜邦线在履带车持续振动环境下有松脱风险，开发阶段可临时使用（胶枪点胶固定），定型后替换为带锁扣的工业连接器。

#### 信号线连接: JST 系列带锁扣插头

| 连接器 | 间距 | 适用场景 | 锁紧方式 |
|--------|------|---------|---------|
| **JST-XH** | 2.5mm | UART/PWM/I2C 信号线组 | 塑料卡扣, 防反插 |
| **JST-PH** | 2.0mm | 紧凑空间信号线 | 塑料卡扣 |
| **JST-SH** | 1.0mm | 空间极度受限处 | 摩擦 + 卡扣 |

**建议线束分组**:

| 线束名称 | 连接器 | 引脚数 | 包含信号 |
|---------|--------|--------|---------|
| VIS 线 (N6→ESP32) | XH 3P | 3 | TX, GND, 预留 |
| MotorCmd 线 (ESP32↔STM32) | XH 4P | 4 | TX, RX, GND, 3V3 |
| ESC PWM 线 (STM32→电调) | XH 3P | 3 | CH1, CH2, GND |
| 急停回路 | XH 2P | 2 | GPIO, GND |
| I2C 传感器线 (ToF) | XH 4P | 4 | SCL, SDA, 3V3, GND |

需要一把压线钳 (SN-28B, ~¥25)。

#### 电源线连接

| 电压域 | 连接器 | 额定电流 | 说明 |
|--------|--------|---------|------|
| 48V 动力线 | **XT60** (黄色) | 60A | 航模标准, 香蕉头, 防反插 |
| 48V→5V 降压器到控制板 | **XT30** (黄色) | 30A | XT60 缩小版 |
| 5V 到各板供电 | **KF128 弹簧端子** (5.08mm) | 15A | 免焊接, 螺丝/弹簧压接 |
| 板载 3.3V | PCB 铜箔 (载板上) | — | 不引出到连接器 |

**安全规则**: 48V 和 5V 必须使用完全不同的连接器类型，防止误插导致 48V 灌入控制板。

#### 调试/经常插拔口: GX12 航空插头

| 规格 | 引脚数 | 直径 | 锁紧 | 防水 |
|------|--------|------|------|------|
| GX12-4P | 4 | 12mm | 螺纹 | 可选 IP67 |
| GX12-6P | 6 | 12mm | 螺纹 | 可选 IP67 |
| GX16-4P | 4 | 16mm | 螺纹 | 可选 IP67 |

- GX12-4P: N6 ↔ ESP32 主线束
- GX12-6P: 调试/烧录口（SWD + UART 合一）
- 淘宝 ~¥3-5/套（公母一对）

#### 终极方案: 定制载板 PCB (定型后)

```
┌──────────────────────────────────────┐
│            载板 PCB (JLCPCB 打样)     │
│  ┌──────────┐  ┌──────────┐          │
│  │ N6 插座  │  │ ESP32    │          │
│  │ (排母)   │  │ 插座     │          │
│  └──────────┘  └──────────┘          │
│  ┌──────────┐  ┌──────────────────┐  │
│  │ STM32    │  │ 对外接口区        │  │
│  │ 插座     │  │ XT30/XH/GX/端子  │  │
│  └──────────┘  └──────────────────┘  │
│    走线全在 PCB 内层，零飞线           │
└──────────────────────────────────────┘
```

- 嘉立创 10×10cm 双面板打样 **¥5 包邮**
- 各板通过排母排针插在载板上，板间通信走 PCB 铜箔
- 对外接口全部走锁紧端子，零杜邦线
- 需要各板尺寸和引脚分配完全定型后才能画

#### 分阶段实施

| 阶段 | 做法 | 成本 | 时机 |
|------|------|------|------|
| **开发调试** | 杜邦线 + 胶枪关键节点点胶 | ~¥15 | Phase 1-5 |
| **联调稳定** | 换 JST-XH 带扣插头 + XT30 电源口 | ~¥50 + 半天 | Phase 7 前 |
| **最终定型** | 画载板 PCB，嘉立创打样 | ~¥50 + 1 天 | Phase 8 后 |

### 0.6 原装系统逆向分析
- [ ] 测绘原车供电走线：电池→配电→控制器→电机的完整回路
- [ ] 确认原装遥控接收机的接口和协议（PWM/PPM/SBUS/2.4G 私有）
- [ ] 标记保留模块：电池、BMS、保险丝盒、主电源开关
- [ ] 标记替换模块：原装双路控制器 → 我们采购的防水电调
- [ ] 确认原车是否已有急停按钮，若有则评估是否可复用

---

## Phase 1 — STM32 底层驱动与安全层 (预计 3-5 天)

**目标**: STM32 可以独立控制履带电机，安全机制全部生效。本阶段不依赖 ESP32 和 OpenMV。

### 1.1 RC PWM 电调控制
- [ ] **先确认电调控制接口协议**（查阅电调说明书：标准 RC PWM 50Hz / UART / CAN）
- [ ] 假设标准 RC PWM：中位 1500μs = STOP, 1500-2000μs = 前进, 1000-1500μs = 后退
- [ ] 配置 TIM1 CH1/CH2 输出 50Hz 标准舵机 PWM 信号到电调左右通道
- [ ] 实现 `void escWrite(CarCmd cmd, uint8_t pwm)` — 将 CarCmd + pwm 映射为 μs 脉宽
- [ ] 映射公式: FWD → `1500 + map(pwm, 0,255, 0,500)` μs; REV → `1500 - map(pwm, 0,255, 0,500)` μs
- [ ] 用示波器或舵机测试器验证 PWM 脉宽正确（中位 ±5μs 精度）
- [ ] 电调上电自检流程：上电时输出中位 STOP，等待电调初始化完成（听蜂鸣声）
- [ ] 履带空载测试：FWD / REV / 原地差速转向 / STOP 各方向正确
- [ ] **注意**: 电调内部已处理 H-bridge 换向和死区，STM32 无需再做死区逻辑

### 1.2 UART 命令接收与解析
- [ ] USART1 中断接收，环形缓冲区
- [ ] 定长二进制协议：`[0xAA][CMD:1B][PWM:1B][CRC8:1B]` 共 4 字节
- [ ] 帧头 0xAA 同步 + CRC8 校验
- [ ] 解析后更新内部 `MotorCmd latestCmd`

### 1.3 命令超时自动停车
- [ ] SysTick 1ms 中断驱动超时计数器
- [ ] 收到有效命令 → 清零计数器
- [ ] 超时 500ms → `motorWrite(STOP, 0)` + 置位超时标志
- [ ] 超时后需收到连续 3 个有效帧才能恢复

### 1.4 硬线急停逻辑
- [ ] GPIO 输入检测急停按钮状态（常闭触点, 断开=急停）
- [ ] GPIO 输出驱动 48V 大电流继电器（常开, 失电=断开电调动力电）
- [ ] 急停回路设计：电池+ → 继电器常开触点 → 电调 48V 输入。继电器线圈由 STM32 GPIO + 急停按钮串联控制
- [ ] 急停触发 → 继电器断开 + PWM 归零 + 上报错误码
- [ ] 恢复流程：急停按钮复位 + ESP32 发送解除命令（或物理钥匙开关）
- [ ] **安全设计**: 急停链路为常闭逻辑 — 断线/断电/STM32死机 任一故障均触发急停

### 1.5 编码器脉冲计数 (可选)
- [ ] TIM2/TIM3 编码器模式配置
- [ ] 速度计算（每 10ms 的脉冲增量 → RPM）
- [ ] 若无编码器则跳过，后续补装

### 1.6 电流采样与过流保护
- [ ] **先确认电调是否有电流回传功能**（UART/CAN 回传或模拟电压输出）
- [ ] 若电调无回传: ADC 采样左右电机电流（ACS712 串联在电调输出到电机之间）
- [ ] 过流阈值设定（根据电机额定电流 ×1.5 持续, ×3 瞬时）
- [ ] 过流触发 → PWM 归零 + 错误码 CURRENT_FAULT 上报 + 可选继电器断开
- [ ] 若电调自带过流保护: STM32 只需监控电调回传的状态字

---

## Phase 2 — 遥控器集成 (预计 3-4 天)

**目标**: ELRS/CRSF 遥控链路打通，Manual 模式可用。此阶段不需要 OpenMV 视觉。

### 遥控设备选型
| 设备 | 推荐型号 | 说明 |
|------|---------|------|
| 遥控器 | **Jumper T14 ELRS** | EdgeTX 系统, 内置 ELRS 2.4G 1W, Hall 摇杆, USB-C 充电 |
| 接收机 | **Radiomaster ER6** / **Happymodel EP1** | ELRS 2.4G, CRSF 输出, 双向遥测 |
| 备选遥控 | Radiomaster Boxer / TX16S | 同样 EdgeTX + ELRS, 屏幕更大 |
| 协议 | **CRSF** (Crossfire) | 420k baud, 16ch × 11-bit, 双向遥测 |

> Jumper T14: EdgeTX 系统, 内置 ELRS 模块最高 1W 发射功率, 开源固件可深度定制混控和逻辑开关。接收机通过 UART 输出 CRSF 协议帧到 ESP32。

### 2.1 ELRS 接收机绑定与 CRSF 库集成
- [ ] UART1 (RX=GPIO15, TX=GPIO4) 接 ELRS 接收机 CRSF 端口（TX↔RX 交叉）
- [ ] 接收机供电：5V（从 48V→5V 降压器取电）
- [ ] 安装 [CRSFforArduino](https://github.com/ZZ-Cat/CRSFforArduino) 库
- [ ] ELRS 发射端和接收机刷相同版本固件，完成 Bind
- [ ] 运行示例代码验证 16 通道数据流正常接收
- [ ] 关键设置: EdgeTX 内部模块 baud = 921k（EdgeTX 默认即可）；ELRS Packet Rate = 250Hz（地面车辆不需要 FPV 无人机的高刷新率）

### 2.2 通道映射与遥控器配置
- [ ] EdgeTX Mixer 配置：
  - **CH1**: 右摇杆 X 轴 → Steering（转向，自回中）
  - **CH2**: 备用（可映射摄像头俯仰或暂留空）
  - **CH3**: 左摇杆 Y 轴 → Throttle（油门，建议设回中弹簧 or 取消回中用棘齿手感）
  - **CH4**: 备用
  - **CH5**: 三档开关 SWA → Mode: Manual / Auto-Follow / Emergency Stop
  - **CH6**: 旋钮 POT1 → Max Speed 限速调节
- [ ] CRSF 通道值范围: 172-1811（中位 992），映射为 -100% ~ 0 ~ +100%
- [ ] 在 ESP32 端实现 `CRSFReader` 类，封装通道值读取

### 2.3 遥控器 → 电机直通控制 (Manual Mode)
- [ ] CH3 油门 → FWD/REV + pwm 值: 中位以上 = 前进, 中位以下 = 后退
- [ ] CH1 转向 → 差速比例: 摇杆左 → 左履带减速/右履带加速, 摇杆右 → 反之
- [ ] 差速映射: `pwm_L = throttle - steer_factor`, `pwm_R = throttle + steer_factor`
- [ ] CH6 旋钮限速: pwm 上限 = map(pot1, 0~100%, MIN_PWM~MAX_PWM)
- [ ] 死区设置: 摇杆中位 ±3% 视为 STOP（避免摇杆抖动导致车微动）
- [ ] MotorCmd 通过 UART2 发送到 STM32
- [ ] 实际开起来验证操控手感和履带差速比例

### 2.4 遥控失联 FailSafe
- [ ] CRSF 协议自带链路质量监控: RSSI (dBm) + LQ (Link Quality %)
- [ ] LQ < 50% 持续 300ms → Dashboard 低信号警告
- [ ] LQ = 0% / 接收机完全失联 → ELRS 接收机内部自动进入 FailSafe（所有通道输出预设的 failsafe 值）
- [ ] EdgeTX 端配置 FailSafe 位置: CH3 回中（=0%油门）, CH5 切到 Emergency Stop
- [ ] ESP32 端额外检测: CRSF 帧中断超过 100ms → 强制 STOP
- [ ] 恢复：信号恢复后需 CH3 回中 + CH5 切回 Manual 才解锁

### 2.5 模式切换 Manual / Auto / STOP
- [ ] CH5 (SWA 三档) 读取: 上=Manual(遥控驾驶), 中=Auto-Follow(视觉跟随), 下=E-Stop(紧急停车)
- [ ] 模式切换时 ESP32 行为仲裁器收到模式变更事件
- [ ] Auto→Manual 切换: 立即切换到遥控驾驶, 无延迟
- [ ] Manual→Auto 切换: 先 STOP 100ms, 确认视觉数据有效后才切入跟随
- [ ] 任何模式 → E-Stop: 立即 STOP + 持续发送 STOP 命令

### 2.6 CRSF 双向遥测（可选增强）
- [ ] ESP32 → ELRS 接收机回传: 电池电压、车速、模式、错误码
- [ ] Jumper T14 屏幕上实时显示遥测数据（通过 EdgeTX Telemetry 页面配置）
- [ ] 利用 EdgeTX Logical Switches 设置: 电压过低振动报警、错误码触发语音提示

---

## Phase 3 — ESP32 ↔ STM32 UART 通信协议 (预计 2-3 天)

**目标**: 双板双向通信稳定可靠，有 CRC、有超时、有心跳。

### 3.1 双向二进制协议设计
```
下行 (ESP32 → STM32, 每 50ms):
  [0xAA] [CMD:1B] [PWM:1B] [CRC8:1B]
  共 4 字节

上行 (STM32 → ESP32, 收到命令后立即回传, 或每 100ms 主动上报):
  [0xBB] [EncL:2B] [EncR:2B] [CurL:1B] [CurR:1B] [Flags:1B] [ErrCode:1B] [CRC8:1B]
  共 10 字节

Flags bit定义:
  bit0: 急停状态
  bit1: 命令超时
  bit2: 过流警告
  bit3: 心跳
ErrCode 错误码:
  0x00: 正常
  0x01: 过流关断
  0x02: 急停触发
  0x03: 命令超时
  0x04: 编码器故障
```

### 3.2 ESP32 端 UART 发送 + 接收
- [ ] USART2 → STM32 发送 MotorCmd（Arduino `Serial2`）
- [ ] USART2 中断接收遥测帧
- [ ] DataAggregator 新增字段：encL, encR, curL, curR, stmFlags, stmErrCode

### 3.3 STM32 端遥测回传
- [ ] 收到下行帧 → 回传当前状态
- [ ] 无下行帧时每 100ms 主动发送心跳
- [ ] 遥测帧同样带 CRC8

### 3.4 心跳 + 通信异常处理
- [ ] ESP32 端检测遥测超时（300ms 无回传 → Dashboard 报警）
- [ ] STM32 端命令超时（500ms → STOP，延续 1.3）
- [ ] UART 帧错误率超过 10% → Dashboard 报警

---

## Phase 4 — ESP32 主决策层 (预计 3-5 天)

**目标**: 视觉跟随功能完整可用，距离保持逻辑正确，行为仲裁优先级清晰。

### 4.1 剥离现有代码
- [ ] `MotorTask.cpp` → 删除 PWM 直驱，改为 UART MotorCmd 发送
- [ ] `DataAggregator.h` → 新增 STM32 遥测字段
- [ ] `Tasks.cpp` → 调整任务结构适应新架构
- [ ] `DataAggregator.h` → 移除 CarState 超声波字段，VisState 新增 tofDistance (mm)
- [ ] 超声波避障代码全部移除（已由 VL53L1X ToF 替代，距离数据由 OpenMV 统一提供）

### 4.2 FollowLogic 重构: 差速转向
- [ ] 输入：VisState（cx偏航, distScore融合距离, tofDistance原始mm）, RC 通道值
- [ ] 输出：MotorCmd（CarCmd + pwm）
- [ ] 转向控制：`pwm_diff = Kp * (cx - FRAME_CX) / FRAME_CX * MAX_PWM`
- [ ] 差速输出：`pwm_L = base_pwm - pwm_diff`, `pwm_R = base_pwm + pwm_diff`
- [ ] 限幅保护：pwm 不超 PWM_RANGE

### 4.3 VisionBridge 集成
- [ ] 确保 VIS 帧正常进入 VisState
- [ ] 合理性校验：w∈[10,320], h∈[10,240], conf∈[0,1]
- [ ] 异常数据过滤 + 计数上报

### 4.4 行为仲裁器
```
优先级链（从高到低）:
1. 急停 (STM32 硬线 + 遥控CH5=3)
2. STM32 错误码 (过流/编码器故障)
3. 遥控失联
4. 命令超时 (STM32 端已处理，ESP32 不重复发)
5. 视觉超时 (VIS 帧 > VISION_TIMEOUT_MS 未更新)
6. 遥控 Manual Mode
7. 视觉跟随 Auto-Follow Mode
8. 默认: STOP
```

### 4.5 距离保持逻辑

distScore 由 OpenMV 端融合计算：视觉特征 (area_ratio + feetY) + VL53L1X ToF 距离 (mm)。
ESP32 端仅消费最终 distScore，阈值映射如下：

| distScore 范围 | ToF 参考距离 | 行为 | 命令 |
|----------------|-------------|------|------|
| < 0.25 (太远) | >3m / 无效 | 全速前进追赶 | FWD, pwm=MAX |
| 0.25-0.45 (较远) | ~1.5-3m | 中速跟进 | FWD, pwm=120 |
| 0.45-0.60 (合适) | ~1-1.5m | 微调保持 | FWD/STOP 交替 |
| 0.60-0.80 (较近) | ~0.5-1m | 慢速后退 | REV, pwm=80 |
| > 0.80 (太近) | <0.5m | 全速后退 | REV, pwm=MAX |

滞后设计（防振荡）：
- 进入"合适"区后需偏离 ±0.05 才触发移动
- 状态转换需维持 3 帧（~600ms）确认
- ToF 有效时 (40-4000mm) distScore 以 ToF 为主要权重；ToF 无效时降级为纯视觉估计

---

## Phase 5 — ESP32 远程监控与调试 (预计 2-3 天)

**目标**: 手机连 WiFi 就能看到所有状态、在线调参、导出日志。

### 5.1 WiFi AP Dashboard 完善
- [ ] 现有 `DashboardServer` 扩展遥测显示
- [ ] 页面布局：左侧状态面板 + 右侧实时数据
- [ ] 显示：模式、distScore、tofDistance (mm)、编码器速度、电流、错误码、RSSI
- [ ] 手机端适配（viewport meta, 大字体）

### 5.2 WebSocket 实时遥测
- [ ] 每 100ms 推送 JSON 数据包
- [ ] JSON 字段：`{ts, mode, carCmd, pwm, encL, encR, curL, curR, distScore, tofDist, cx, conf, flags, errCode, uartErrors}`
- [ ] Dashboard 用 Chart.js 画电流/速度时序图

### 5.3 在线参数调整
- [ ] HTTP API: `POST /api/config?key=DIST_THRESHOLD_FAR&value=0.25`
- [ ] API 列表：所有 follow 阈值、PID 参数、速度限制
- [ ] 保存到 ESP32 NVS → 重启保持
- [ ] Dashboard 提供参数调整面板（滑块 + 输入框）

### 5.4 日志记录与回传
- [ ] 环形缓冲区（最近 2000 条事件）
- [ ] 事件类型：模式切换、错误发生恢复、视觉丢失恢复、参数变更
- [ ] `GET /api/logs?last=500` → JSON 数组导出
- [ ] Dashboard 提供下载 CSV 按钮

---

## Phase 6 — OpenMV 视觉 + ToF 测距 (预计 3-4 天)

**目标**: OpenMV 视觉管线 + VL53L1X ToF 激光测距针对履带车场景校准完成，输出融合后的可靠距离估计。

### 6.1 VL53L1X ToF 测距集成
- [ ] 安装 OpenMV 测距扩展板 (星瞳科技, ¥136, VL53L1X) 到 OpenMV Shield 接口
- [ ] I2C 扫描确认 VL53L1X 在线 (addr 0x29), 使用 `vl53l1x` 内置模块
- [ ] 验证 ToF 测距范围 40-4000mm, 精度 ±1mm, 最大 50Hz 读取率
- [ ] 测试不同光照条件 (室内/户外/逆光) 下 ToF 测距稳定性
- [ ] 确认与 UART3 (P4 TX) 无引脚冲突 — 测距扩展板使用独立 I2C Shield 总线
- [ ] N6 兼容性确认: 如 Shield 接口不同, 使用 I2C(1) 飞线连接 (P7 SCL / P8 SDA)

### 6.2 Person Detection 验证
- [ ] 履带车低视角场景（离地约 50-80cm）测试
- [ ] 不同距离（1m/2m/3m/5m）检测率统计
- [ ] 若检测率 < 80%（3m内）→ 尝试降低 DETECTION_THRESHOLD 或换更大的 YOLO 模型

### 6.3 VIS 协议扩展 (新增 tofDistance 字段)
- [ ] VIS 帧格式扩展: 在数据串末尾增加 ToF 距离字段 `tofDist`
  - 有人: `cx,cy,w,h,feetY,conf,PERSON,distScore,tofDist`
  - 无人: `0,0,0,0,0,0.00,NONE,0.00,0`
- [ ] XOR checksum 覆盖新增字段
- [ ] ESP32 VisionBridge 同步更新解析逻辑
- [ ] 用 USB 串口同时监听确认帧内容正确

### 6.4 距离融合算法设计与校准

**融合策略**:
```
ToF 有效 (40-4000mm):
  tof_score = 1.0 - (tofDist - 400) / (4000 - 400)  // 映射到 0-1 (越远越小)
  tof_score = clamp(tof_score, 0.0, 1.0)
  distScore = tof_score * 0.7 + visual_score * 0.3     // ToF 主, 视觉辅

ToF 无效 (out of range / sensor error):
  distScore = visual_score                               // 纯视觉降级
  (沿用现有 AREA_CLOSE/AREA_FAR 多特征融合逻辑)
```

- [ ] 在履带车上实际测量不同距离 (0.5m/1m/1.5m/2m/3m/4m) 下的 ToF 读数
- [ ] 标定 ToF 安装角度和视野遮挡情况（是否被车体遮挡近距离地面）
- [ ] 验证 ToF 读数与真实距离的线性度
- [ ] 重新标定视觉侧 AREA_CLOSE / AREA_FAR / AREA_VERY_CLOSE 阈值 (履带车视角)
- [ ] 确定 ToF 有效范围 (40-4000mm) 与视觉有效范围的重叠区间
- [ ] 联调融合权重: 调整 ToF vs 视觉的权重分配，测试典型跟随场景
- [ ] 处理 ToF 跳变/噪声: 中值滤波 (3 样本滑动窗口) 或低通滤波
- [ ] 40mm 盲区 (极近距离) 处理: 仅依赖视觉 area_ratio + feetY 判断 STOP

---

## Phase 7 — 全系统联调 (预计 3-5 天)

**目标**: 三板协同工作，端到端链路打通，PID 整定完成，安全测试全覆盖。

### 7.1 上电启动顺序
- [ ] 启动流程：STM32 先上电自检（PWM 锁定 0）→ ESP32 上电连接 → 握手 → 解锁
- [ ] 握手协议：ESP32 发 `HANDSHAKE_REQ` → STM32 回 `HANDSHAKE_ACK` + 版本号
- [ ] OpenMV 最后手动上电（或由 ESP32 GPIO 控制上电），启动慢不影响安全

### 7.2 端到端链路验证
- [ ] 人站在摄像头前 → 确认 OpenMV 检测 → ESP32 决策 → STM32 驱动
- [ ] 全链路延迟测试：人在摄像头前移动 → 车轮响应时间 < 300ms 为合格
- [ ] 静态跟随测试：人保持不动，车保持距离稳定（不前后振荡）

### 7.3 PID 整定
- [ ] STM32 速度环 PID：P（比例增益）→ I（消除静差）→ D（抑制超调）
- [ ] ESP32 FollowLogic 转向 P 控制器（纯比例通常够用）
- [ ] 记录最优参数到 Config.h + NVS

### 7.4 安全机制测试清单
| 测试项 | 操作 | 预期结果 |
|--------|------|---------|
| 急停按钮 | 运行中按下 | 继电器断开，车立即停 |
| 过流保护 | 堵转电机 | <1s 内 PWM 归零 |
| 命令超时 | 拔掉 ESP32-STM32 UART 线 | <500ms 车自动停 |
| 遥控失联 | 运行中关遥控器 | <100ms 车自动停 |
| 视觉超时 | 运行中遮住摄像头 | <700ms 车自动停 |
| 遥控恢复 | 重新开遥控器 | 需手动回中后解锁 |

---

## Phase 8 — 场地测试与迭代 (预计 5-7 天)

**目标**: 真实环境验证系统可靠性和鲁棒性。

### 8.1 室内平地跟随
- [ ] 光滑地面直线跟随（1m/s 慢速）
- [ ] 90° 转弯跟随（人转弯车能否跟上）
- [ ] 后退保持测试（人走向车）
- [ ] 记录指标：跟踪丢失率、距离保持误差

### 8.2 户外/坡道测试
- [ ] 水泥路面 → 草地 → 砂石路面
- [ ] 10° 缓坡启停（车能否在坡道上静止不滑坡）
- [ ] 颠簸路面视觉稳定性（摄像头是否抖到丢失目标）

### 8.3 紧急场景测试
- [ ] 人突然从侧面闯入（检测响应时间）
- [ ] 目标突然消失（车是否平稳停止）
- [ ] 电机堵转（过流保护响应）
- [ ] 通信线缆松脱（系统是否安全停车而非失控）

### 8.4 续航与散热
- [ ] 满载等效负载连续运行（2×500W 峰值 → 48V 89Ah 理论续航约 4 小时 @ 50% 负载）
- [ ] 电池电压从满电 (~54.6V) 到截止 (~36V)，记录实际运行时间
- [ ] 电调散热片温度（红外测温枪，持续满负载工况）
- [ ] 48V→5V 降压模块温度（10A 额定，实际控制电路 <3A，余量充足）
- [ ] STM32/ESP32 芯片温度
- [ ] 若过热 → 加散热片/风扇（电调位置需通风）

---

## 依赖关系图

```
Phase 0 (初始化)
  │
  ├── Phase 1 (STM32 底层) ── 可与 Phase 2 部分并行
  │     │
  │     ├── Phase 2 (遥控器) ── 可与 Phase 6 并行
  │     │     │
  │     │     ├── Phase 3 (通信协议)
  │     │     │     │
  │     │     │     ├── Phase 4 (ESP32 决策) ── 可与 Phase 5 部分并行
  │     │     │     │     │
  │     │     │     │     ├── Phase 5 (Dashboard)
  │     │     │     │     │
  │     │     │     │     ├── Phase 7 (联调)  ◄── 需要 Phase 6 完成
  │     │     │     │           │
  │     │     │     │           └── Phase 8 (场地测试)
  │     │     │     │
  │     │     │     └── (依赖 Phase 4 的 DataAggregator 定义)
  │     │     │
  │     │     └── (遥控直通依赖 Phase 3 协议)
  │     │
  │     └── Phase 6 (OpenMV 视觉) ── 可与 Phase 1-5 并行
  │           (依赖 Phase 4.3 VisionBridge 集成时联调)
  │
  └── 0.5 电气拓扑 ── 指导所有后续硬件连接
```

## 里程碑

| 里程碑 | 完成 Phase | 标志性成果 |
|--------|-----------|-----------|
| M1: 车能遥控开起来 | 2 | 遥控器控制履带车前进/后退/转向 |
| M2: 安全层完整 | 1+3 | 所有 5 条安全链路可独立关断动力 |
| M3: 能视觉跟随 | 4+6 | 人走车走，人停车停，人退车退（视觉+ToF融合距离） |
| M4: Dashboard 可用 | 5 | 手机浏览器看所有状态、在线调参 |
| M5: 全系统可靠 | 7 | 安全测试全通过，端到端 <300ms |
| M6: 可以出门 | 8 | 户外各种地面稳定跟随 |
